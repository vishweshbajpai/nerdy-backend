export enum StatusCodes {
  Ok = "200",
  BadRequest = "400",
  Unauthorized = "401",
  Forbidden = "403",
  NotFound = "404",
  InternalServerError = "500",
}

export enum Errors {
  TOKEN_EXPIRED_ERROR = "TokenExpiredError",
}
